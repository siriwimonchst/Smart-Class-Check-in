name: smart_checkin_app
description: Smart Class Check-in & Learning Reflection App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  google_fonts: ^6.2.1
  geolocator: ^12.0.0
  mobile_scanner: ^5.2.3
  shared_preferences: ^2.3.2
  uuid: ^4.4.2
  # ถ่ายรูปด้วยกล้องหรือเลือกจากคลัง
  image_picker: ^1.0.7
  firebase_core: ^4.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
